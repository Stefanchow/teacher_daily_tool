import zh from '../locales/zh.json';
import en from '../locales/en.json';

/**
 * Application Localization Constants (Chinese)
 * Used for all UI text to ensure consistency and easy localization.
 */
export const TEXT_ZH = zh;

export const TEXT_EN = en;
